# Sandro Tsukhishvili — Portfolio

Static portfolio. Publish only the `docs` directory with GitHub Pages (branch deployment, /docs).

Original project PDFs and the original résumé are excluded by .gitignore. Do not force-add them. Public project PDFs in docs/assets are anonymized, image-only derivatives; the résumé is an intentional public copy. Serve docs only for local preview, never the repository root.

Local preview: python -m http.server 8080 --directory docs

No external scripts, fonts, analytics, dependencies or paid services.
